export interface Organization{
    org_id: Number,
    description: String,
    name: String,
}

export interface OrganizationWithCategory{
    org_id: Number,
    name: String,
    category: String,
}